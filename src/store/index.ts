import { createStore } from 'vuex';
import authModule from '@/store/authModule';

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    authModule,
  },
});
